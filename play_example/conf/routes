# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Utils
GET      /health-check                           controllers.Application.healthCheck
POST     /create-schemas                         controllers.Application.createSchemas
POST     /populate-database                      controllers.Application.populateDatabase


# Tasks
GET      /v1/tasks                               controllers.TasksController.tasks

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
